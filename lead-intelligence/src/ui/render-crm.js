/**
 * CRM View Renderer — Phase 6
 */
export async function renderCRM() {
    const el = document.getElementById('crm-view');
    el.classList.remove('hidden');
    el.innerHTML = '<div style="text-align:center;padding:40px;color:#86868b">CRM wird in Phase 6 implementiert.</div>';
}
